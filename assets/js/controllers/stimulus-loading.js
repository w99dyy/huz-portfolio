// Stimulus loading helper
export function eagerLoadControllersFrom(controllers, application) {
  // This loads all controllers from the controllers folder
  // For Jekyll, we need to handle the dynamic imports differently
  console.log("Loading controllers from:", controllers)
}

// Better approach: import controllers manually in index.js
